[project]
name = "cookiefypm"
version = "0.1.0"
description = "Cookiefy Package Manager project"
authors = [
    {name = "Ferran Colome",email = "ferrancolomsanz@gmail.com"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "playwright (>=1.57.0,<2.0.0)",
    "pyyaml (>=6.0.3,<7.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
